# ==========================================================
# POPPER-PROOF COURT — Backend Dependencies
# Python 3.11+ recommended. Install with:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# ==========================================================

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.3
python-multipart>=0.0.12

# --- Settings & validation ---
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# --- HTTP clients ---
httpx>=0.28.0
tenacity>=9.0.0

# --- LLM providers ---
openai>=1.55.0
anthropic>=0.40.0

# --- Agent orchestration ---
langgraph>=0.2.50
langchain-core>=0.3.20
langchain-openai>=0.2.10
langchain-anthropic>=0.3.0

# --- Voice (ElevenLabs) ---
elevenlabs>=1.50.0

# --- Vector store & embeddings (for feedback loop / Appellate Court) ---
chromadb>=0.5.20

# --- Persistence ---
aiosqlite>=0.20.0

# --- Dev / quality ---
ruff>=0.8.0
